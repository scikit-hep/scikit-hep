language: python

os:
  - linux

python:
  - 2.7
  - 3.6
  - 3.8

install:
  - pip install --upgrade pip
  - pip install coveralls pytest pytest-cov

script:
  - pip install .
  - pytest --cov=skhep tests

after_success:
  coveralls

notifications:
  slack: scikit-hep:b6cgBXwccPoaCNLn5VKFJFVy
